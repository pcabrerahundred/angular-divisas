import { NumberDirectiveDirective } from './number-directive.directive';

describe('NumberDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
